---

title: Installing MyProject on Ubuntu
tags: installation,ubuntu

---

To get MyProject installed on Ubuntu, follow the steps below:

<% current_page.children.sort_by(&:url).each_with_index do |step, i| %>
- **[Step <%= i %>](<%= step.url %>)**: <%= step.data.title %>
<% end %>

Once you have all of these steps complete you should be setup and ready to
using MyProject.

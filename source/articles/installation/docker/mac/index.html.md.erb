---

title: Installing Doubtfire on macOS using Docker 
tags: installation,mac, Docker 

---

To get Doubtfire installed on macOS using Docker, follow the steps below:

<% current_page.children.sort_by(&:url).each_with_index do |step, i| %>
- **[Step <%= i+1 %>](<%= step.url %>)**: <%= step.data.title %>
<% end %>

Once you have all of these steps complete you should be setup and ready to
using Doubtfire.